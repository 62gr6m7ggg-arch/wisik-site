{
  "name": "wisik-site",
  "version": "0.1.7",
  "private": true,
  "description": "Wisik.nl — festivalterrein voor rekenen en wiskunde",
  "scripts": {
    "audit:update": "node scripts/audit-pabo-release.mjs --write",
    "audit:verify": "node scripts/audit-pabo-release.mjs",
    "check": "npm run audit:verify && node scripts/check-site.mjs && node scripts/test-feedback.mjs && node scripts/test-pabo-navigation.mjs && node scripts/test-backstage-roadie.mjs && node scripts/test-backstage-audit.mjs",
    "build": "npm run check",
    "preview": "npx wrangler pages dev public",
    "predeploy": "npm run check",
    "deploy": "npx wrangler pages deploy public --project-name wisik-site"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
